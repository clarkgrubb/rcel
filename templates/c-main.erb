#include <errno.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>

<% header_lines.each do |header| %>
<%= header %>
<% end %>

<% class_lines.each do |line| %>
<%= line %>
<% end %>

int
main (int argc, char **argv) {
  <% main_lines.each do |line| %>
  <%= line %>
  <% end %>
  return 0;
}
